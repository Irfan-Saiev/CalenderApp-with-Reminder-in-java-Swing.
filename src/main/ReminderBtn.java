/**
 *
 * @author Irfan Saiev
 */

package main;

import javax.swing.JButton;

// Class representing a customized JButton for reminders
public class ReminderBtn extends JButton {
    
    // Constructor to initialize the button with text
    public ReminderBtn(String text) {
        super(text); // Call the constructor of the superclass (JButton) with the specified text
    }
}
